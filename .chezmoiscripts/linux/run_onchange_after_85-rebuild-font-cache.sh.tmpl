{{ if eq .chezmoi.os "linux" -}}
#!/bin/sh

# Last Modified (Epoch): {{ output "stat" "--format=%Y" ( joinPath .chezmoi.homeDir ".local/share/fonts" ) | trim }}

echo "Rebuilding font cache..."
fc-cache -f -v "$HOME/.local/share/fonts"
{{- end }}
